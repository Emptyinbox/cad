---
layout: layouts/base.njk
---
<article class="prose">
  <header class="page-header" style="background:none;border:0;padding:0 0 1rem;">
    <h1>{{ title }}</h1>
    {% if description %}<p class="page-header__lede">{{ description }}</p>{% endif %}
  </header>
  {{ content | safe }}
</article>

{% include "partials/cta.njk" %}
